name: calculator
description: Calculator App.
publish_to: 'none'
version: 2.0.1+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  auto_size_text: ^3.0.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true